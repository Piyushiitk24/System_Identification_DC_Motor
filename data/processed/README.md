# Processed Data

Store computed outputs derived from raw measurements here.

Examples:

- aggregate forward/reverse static maps
- mean and standard deviation across repeated runs
- fitted static compensation tables
- derived quantities used by notebooks

Do not manually enter bench readings here. Raw measured values belong in
`data/raw/`.
